# QuantumTicTacToe

## Install: 
- using yarn: `yarn install`
- using npm: `npm install`

## Run:
- using yarn: `yarn start`
- using npm: `npm start`

## Test: 
- using yarn: `yarn test`
- using npm: `npm test`
